# Sandbox-phantom orphans from spark-fsm session 1 (2026-05-09)
# These dirs exist on disk but were never wired into build.py (the canonical pipeline).
# Ignoring them prevents `git add -A` from accidentally landing them.
# To remove from disk: run `rm -rf _partials/ assets/ locations-we-serve/bethany/` from a real shell on the laptop.
_partials/
assets/

# Standard Python / build noise
__pycache__/
*.pyc
.DS_Store

# Vercel local project link (deployment-config-as-state, not source)
.vercel/
