# CLAUDE.md — sparkshark-com

## Repo Purpose
Public marketing website for **Spark Shark Electric** — sparkshark.com.

A static HTML site with **no frameworks, no JS bundlers, no Node toolchain**. Every HTML file in the repo is generated by `build.py` (Python stdlib only) from a single in-script manifest. Output is committed alongside the generator so GitHub Pages can serve straight from `main`.

Pages covered: homepage, ~15 service pages, ~9 city/location pages, blog posts, plus standard footer pages (about, contact, reviews, FAQ, privacy, terms, 404). Every page emits a 4-node JSON-LD `@graph` (WebSite + Organization + LocalBusiness/Electrician + FAQPage); service pages add BreadcrumbList + Service; blogs add BreadcrumbList + Article.

## Business Context
This is the primary lead-generation surface for Spark Shark Electric (residential electrical, OKC metro). It carries the NAP, license, BBB info, ServiceTitan Scheduler Pro embed (online booking), and all SEO/GEO content. Anything that breaks schema, NAP consistency, or the scheduler embed directly hurts lead flow.

## Current Deployment
- **Production URL:** https://www.sparkshark.com (DNS cutover pending — see Known Blockers)
- **Preview URL:** https://sparksharkelectric.github.io/sparkshark-com/
- **Hosting:** GitHub Pages, free tier, served from `main` branch root
- **Repo:** https://github.com/SparkSharkElectric/sparkshark-com
- **Custom domain:** controlled by a `CNAME` file at repo root (currently removed for github.io preview — restore `www.sparkshark.com` at DNS cutover)
- **Database:** None
- **Important integrations:**
  - ServiceTitan Scheduler Pro (embedded via `ST_API_KEY` + `ST_SCHEDULER_ID` constants in `build.py`)
  - GitHub Pages auto-deploy on push to `main`

## Common Commands
```bash
# install — none. Python 3 stdlib only. No npm/pip install needed.

# build (PREVIEW — github.io project URL, asset paths prefixed with /sparkshark-com)
python3 build.py

# build (PRODUCTION — custom domain, root-relative asset paths)
BASE="" python3 build.py

# extract editable copy drafts from build.py into copy-drafts/*.md
python3 extract-copy-drafts.py

# local preview (any static server works; Python's built-in is fine)
python3 -m http.server 8000
# then open http://localhost:8000 (uses production-style root paths;
# for github.io-style paths, set BASE=/sparkshark-com when running build.py)

# test — no automated test suite. Manual smoke checks (see Acceptance Checks).

# lint — none configured.

# deploy — git push to main. GitHub Pages publishes within ~30s.
git add -A && git commit -m "..." && git push origin main
```

## Quality Gate (`qa.py`)

Pure-Python content-quality checker that runs after every build. Verifies each generated HTML page against an 8-rule registry: scaffolding leaks (`(centered, large)` in headings, `=== SECTION:` markers), unfilled `[VERIFY: …]` placeholders, orphan label bullets, stale strings (Flanco Electric, prior phone numbers, prior addresses), brand misspellings (`SparkShark`/`Sparkshark` flagged), NAP presence (phone / city / ZIP / license on every page), license placement (`#163603` only inside `<footer>`; JSON-LD ignored), and required marketing assets (mascot + logo `<img>` on primary marketing pages).

```bash
# After editing copy / build.py
python3 build.py                 # rebuild the HTML
python3 qa.py                    # full sweep — every sitemap page + /404.html
python3 qa.py /reviews/          # check specific page(s)
python3 qa.py --ci               # machine-readable for CI logs
python3 qa.py --strict           # promote warnings (e.g., `owner`) to failures
bash tests/test-detection.sh     # canary regression test (must PASS)
```

`qa.py` reads `sitemap.xml` for the URL manifest and writes `qa-report.md` at repo root. Sitemap entries with no on-disk file (e.g., zombie dirs from de-listed pages) are skipped with a stderr warning, not failed — sitemap consistency is `build.py`'s responsibility.

**CI gate:** `.github/workflows/qa-on-pr.yml` runs `build → qa --ci → tests/test-detection.sh` on every PR + push to `main`. Any FAIL blocks merge. The full `qa-report.md` uploads as a workflow artifact (30-day retention). On failure, a structured comment is auto-posted to the PR with the per-page breakdown.

**When CI is red:**
1. Open the PR; the auto-posted "QA gate failed" comment has the per-page breakdown inline.
2. If a real content bug fired, fix the source in `copy-drafts/*.md` (or `build.py`), run `python3 build.py`, commit + push. CI re-runs automatically.
3. If `qa.py` is mis-firing on legit copy, mention `@claude` in a PR comment (Claude Code GitHub App) or run `subscribe_pr_activity` from Claude Code on the web — the next Claude session can investigate using this CLAUDE.md as context.

**Adding rules:** Edit the registry at the top of `qa.py` — `STALE_STRINGS`, `SCAFFOLDING_PATTERNS`, `BRAND_MISSPELLINGS`, `DISCOURAGED_WORDS`, `NAP_REQUIREMENTS`, `MARKETING_PAGES`, `PLACEHOLDER_PATTERNS`. Adding a new stale string is a one-line list append. Brand / NAP values must stay in sync with `BRAND` in `build.py` (the dependency is documented in `qa.py`'s docstring).

**Canary regression guard:** `tests/test-detection.sh` injects a known scaffolding bug (`<h2>RATING SUMMARY (centered, large)</h2>` + orphan `Stars:` / `Number:` bullets) into `reviews/index.html`, asserts `qa.py` catches it and names "centered, large" in the report, then restores the file. Runs after `qa.py --ci` in CI. If anyone weakens `qa.py` such that the canary stops firing, the PR is blocked.

## File Map
```text
qa.py                             # Post-build quality gate. See "Quality Gate" above.
tests/test-detection.sh           # Canary regression guard for qa.py.
.github/workflows/qa-on-pr.yml    # CI: build + qa + canary on every PR/push.
build.py                          # SOLE source of truth: page manifest, BRAND dict,
                                  # templates, schema generators. Edit here, then re-run.
extract-copy-drafts.py            # Reads build.py and emits one editable .md per page
                                  # into copy-drafts/. Workflow: edit drafts → merge back
                                  # into build.py → re-run build.py.
copy-drafts/                      # 38 numbered markdown files (01-homepage.md …
                                  # 38-blogs-index.md). Sections per file: TITLE,
                                  # META DESCRIPTION, H1, HERO SUBTITLE, INTRO,
                                  # SECTIONS (h2 + bullets), FAQ (Q/A).
                                  # See "copy-drafts structure" below.

index.html                        # Homepage (generated)
404.html                          # Generated
sitemap.xml, robots.txt           # Generated
llms.txt, llms-full.txt           # Generated AI-crawler manifests

css/site.css                      # Design system + components (hand-written, NOT generated)
js/site.js                        # Minimal interactivity, e.g. mobile menu (hand-written)
img/                              # Logo, mascot, static assets

# Generated page directories — one folder per clean URL, each containing index.html:
about-us/                         contact-us/             reviews/
frequently-asked-questions/       privacy-policy/         terms-and-condition/
services/                         services/emergency-electrician/
electrical-panels/                generators/             electrical-repair-and-service/
electrical-installation/          electrical-inspection-services/
smoke-detectors/                  switches-and-outlets/   ceiling-fans/
indoor-lighting-installation/     electrician-for-outdoor-lighting/
ev-charger-installation/          smart-home-installation/
residential-electrical-solutions/ commercial-electrical-solutions/
industrial-electrical-solutions/

locations-we-serve/               # Index + one folder per city (bethany, del-city,
                                  # edmond, midwest-city, moore, mustang, newcastle,
                                  # norman, yukon). Note: oklahoma-city/ and moore/
                                  # also exist at repo root as standalone city pages.
oklahoma-city/   moore/

blogs/                            # Blog index
2026/05/[slug]/index.html         # Blog post permalinks (also legacy 2023/, 2024/ dirs)

home-staging-2026-05-07/          # Snapshot of pre-launch staging (do not edit)

# Reference / research (not part of the deployed site, can stay or move):
spark_shark_final_merged_seo_geo_research_document.md
Perplexity Validation Report for Spark Shark SEO GEO Brief.md
README.md                         # Public-facing repo README
__pycache__/                      # Python bytecode — ignore
```

### copy-drafts structure
`copy-drafts/` is the **human-editable layer** for site copy. Each file is one page, named with a 2-digit prefix that controls reading order:

```text
01-homepage.md
02-electrical-panels.md … 15-residential-electrical-solutions.md   # service pages
16-services-index.md
17-about-us.md     18-contact-us.md     19-reviews.md     20-faq.md
21-privacy-policy.md     22-terms-and-condition.md
23-locations-index.md
24-oklahoma-city.md … 33-newcastle.md                              # city pages
34-blog-power-out.md … 37-blog-generator-installation.md           # blog posts
38-blogs-index.md
```

Per-file format (enforced by `extract-copy-drafts.py`):
```text
# TITLE (browser tab)
# META DESCRIPTION (Google snippet)
# H1
# HERO SUBTITLE
# INTRO
# SECTIONS (h2 + bullets)
# FAQ (Q/A pairs)
```

**Workflow:** edit the `.md` files → merge changes back into `build.py`'s manifest → re-run `python3 build.py` → commit both the build.py change and the regenerated HTML in the same commit.

## Environment Variables
Do not paste secrets. List names only.
```text
BASE — asset URL prefix for build.py.
       "" for production (custom domain). "/sparkshark-com" for GitHub Pages
       project preview. Defaults to "/sparkshark-com".
```
The site itself ships **no runtime secrets** — it's static HTML. The two ServiceTitan IDs in `build.py` (`ST_API_KEY`, `ST_SCHEDULER_ID`) are the public widget identifiers ServiceTitan provides for embedding the Scheduler Pro widget on a "Custom Website"; they are not secret credentials. If those ever change, update them in `build.py` and re-run.

## Guardrails
- Inspect `build.py` before editing any HTML — most HTML changes belong in `build.py` (or `copy-drafts/` → `build.py`), not in the generated `index.html` files. Hand-edits to generated HTML will be overwritten on the next `python3 build.py`.
- State exact files before modifying.
- Do not refactor unrelated areas (no Jekyll, no Hugo, no Node, no React — the "no frameworks" stance is intentional for GitHub Pages reliability).
- Do not add frameworks or dependencies without approval.
- Do not commit/push/deploy unless Brock approves. (GitHub Pages deploys on every push to `main`, so a push IS a deploy.)
- Preserve the 4-node `@graph` schema architecture and NAP consistency on every page.
- Spark Shark claim rules from global CLAUDE.md apply: do not invent reviews, ratings, BBB status, license info, response times, or testimonials. The numbers in `BRAND` (`build.py`) are the single source of truth — if they need to change, change them there and regenerate.

## Do Not Touch Without Explicit Approval
```text
build.py → BRAND dict                    # NAP, phone, license, rating, review count,
                                         # BBB date. Single source of truth — wrong
                                         # values propagate to every page + every
                                         # JSON-LD graph.

build.py → schema generators             # 4-node @graph, BreadcrumbList, Service,
                                         # Article. Breaking schema breaks SEO/GEO.

build.py → ST_API_KEY, ST_SCHEDULER_ID   # ServiceTitan Scheduler Pro embed. Wrong
                                         # values = broken online booking = lost leads.

CNAME (when present)                     # Controls custom domain. Currently absent
                                         # (preview mode) — restore at DNS cutover.

home-staging-2026-05-07/                 # Frozen pre-launch snapshot. Reference only.

css/site.css                             # Hand-authored. Edit deliberately; no
                                         # auto-formatter is configured.

robots.txt, sitemap.xml, llms.txt,       # Generated — edit via build.py, never by hand.
llms-full.txt
```

## Acceptance Checks
A change is done when:
```text
1. python3 build.py runs cleanly with no Python errors.
   (And BASE="" python3 build.py also runs cleanly if production paths matter
   for this change.)

2. `git status` shows the regenerated HTML matches what was intended — no
   stray files, no half-regenerated pages, no leftover preview-mode asset paths
   if the change is destined for production.

3. `git diff` on a sample generated page (e.g. index.html and one service page)
   reflects the intended copy/markup change and nothing else.

4. JSON-LD on at least one affected page validates as expected (4-node @graph
   present; BreadcrumbList + Service on service pages; Article on blog posts).
   Quick visual check: search the page source for `"@graph"` and confirm the
   four node @types are still WebSite, Organization, Electrician, FAQPage.

5. Local smoke test: `python3 -m http.server 8000`, open the changed page(s),
   confirm: header/footer render, phone link is `tel:+14054364776`, mobile
   menu opens, ServiceTitan scheduler embed loads (homepage + contact),
   internal nav links resolve (no 404s under the BASE used).

6. NAP unchanged unless the task explicitly changes it: phone (405) 436-4776,
   address Moore OK 73160, license #163603.

7. After push to main, wait ~30s and verify the live page (preview or prod URL,
   whichever is active) reflects the change.
```

## Rollback
```text
Single bad commit, not yet built upon:
    git revert <sha> && git push origin main
    # GitHub Pages republishes the prior state in ~30s.

Multiple bad commits in a row, recovering to a known-good SHA:
    git revert <oldest_bad_sha>^..<newest_bad_sha> && git push origin main
    # Creates a clean revert commit chain. Do NOT force-push main.

Broken build.py output (HTML went sideways):
    git checkout <good_sha> -- <path/to/file_or_dir>
    git commit -m "rollback: restore <area> from <good_sha>"
    git push origin main

Wrong BASE baked into a production build (asset paths point at
/sparkshark-com on the live custom domain, or vice versa):
    BASE="" python3 build.py    # for production
    # or
    python3 build.py            # for github.io preview (default BASE)
    git add -A && git commit -m "fix: rebuild with correct BASE" && git push

CNAME / domain rollback:
    Re-add or remove the CNAME file at repo root and push.
    (See d59773f and 0fd1c17 in git log for the cutover pattern.)

Never rewrite history on main (no force-push, no rebase of pushed commits).
```

## Current Known Blockers
```text
- DNS cutover from github.io preview to www.sparkshark.com pending.
  When cutting over: (a) re-add CNAME with `www.sparkshark.com`,
  (b) rebuild with BASE="" so asset paths are root-relative,
  (c) commit + push, (d) verify live site.
```

## Current Priority
```text
Get the new website live at www.sparkshark.com.

Copy drafts are complete. Remaining work is the cutover itself:
  1. Merge any final copy-drafts/ edits into build.py (if not already done).
  2. Re-add CNAME at repo root with: www.sparkshark.com
  3. Rebuild for production:  BASE="" python3 build.py
  4. Commit + push to main.
  5. Verify DNS resolves to GitHub Pages and HTTPS cert provisions.
  6. Smoke-test live site: schema, scheduler embed, phone links, internal nav.
```
