# Seed prompt vs. current truth — diff only

**Source:** `uploads/migrate-day-go-no-go-seed-prompt.md`
**Compared against:** repo HEAD `0ce83e5` on branch `cutover/launch-gate-notes-autonomous-2026-05-11`, `main` HEAD `1d7c4c0`, plus existing audit at `audits/migration-readiness/2026-05-11.md` (run 16:24 UTC).
**Mode:** read-only · diff only · no files changed, no skills invoked.

This file lists only the deltas — the places the seed prompt's assertions or instructions disagree with what the repo, the launch-gate, and the most recent migration-auditor run actually say. It does not re-run any audit and does not produce a verdict. Items not listed here are either already true or out of scope for a diff pass.

---

## A. Hard factual diffs (seed prompt is wrong about something static)

### A1. Target stack

| Seed prompt | Current truth |
|---|---|
| "Target stack: **Next.js** on Vercel" (line 13) | Static HTML site generated by `build.py` (Python stdlib only). No Node, no Next.js, no bundler. `vercel.json` `buildCommand: BASE="" python3 build.py`, `outputDirectory: "."`. No `package.json` exists at repo root. (`~/Projects/sparkshark-com/CLAUDE.md` "Repo Purpose"; `~/Projects/sparkshark-com/vercel.json`.) |

**Why it matters:** any sub-skill that branches on "is this a Next.js cutover?" (e.g. checking `next.config.js`, ISR, edge middleware, dynamic routes) will look for things that do not exist and may falsely flag UNVERIFIED. Tell the orchestrator "static HTML built by a Python script committed alongside output; no Next.js" before it starts.

### A2. New legal entity name

| Seed prompt | Current truth |
|---|---|
| "new entity = **Spark Shark Electric LLC**" (line 16) | Legal entity = **BSF Investment Group, LLC**. Trade names / DBAs = **Spark Shark; Spark Shark Electric**. Public brand = **Spark Shark Electric** (with a space). (`~/Projects/spark-fsm/.claude/rules/business-rules.md` "Brand".) |

**Why it matters:** if site-hygiene-audit's takeover mode generates schema corrections or footer / privacy / terms rewrites under the wrong legal entity, it will inject a new wrong name on top of the old wrong name. The correct schema `legalName` is `BSF Investment Group, LLC`; the correct `name` is `Spark Shark Electric`.

### A3. Old phone (405) 796-8111 — not a blanket "scrub everywhere"

| Seed prompt | Current truth |
|---|---|
| Implies old phone `(405) 796-8111` should be removed everywhere in takeover mode (line 16). | Memory `project_privacy_policy_405_796_8111.md` is explicit: **leave `(405) 796-8111` intact on `/privacy-policy/`** for SMS opt-out compliance (Flanco-era subscriber list). Elsewhere on the site it is a stale leak and should not surface. |

**Why it matters:** the site-hygiene-audit BLOCKER list must whitelist `/privacy-policy/` for this string; otherwise a fix PR will remove a compliance-relevant disclosure.

### A4. Reference runbook in uploads

| Seed prompt | Current truth |
|---|---|
| "Reference runbook I just uploaded … `migration-risk-defense-runbook-2026-05-11.html` in this session's uploads" (line 19) | Uploads folder for this Cowork session contains **only** `migrate-day-go-no-go-seed-prompt.md`. The HTML runbook is not present in `local_dad93d60-…/uploads/`. |

**Why it matters:** sub-skills told to treat the HTML as supporting context will silently skip it. Either re-attach it or tell the orchestrator the runbook is not available this session.

### A5. Cutover runbook location

| Seed prompt | Current truth |
|---|---|
| Decision discipline requires "`docs/migration/cutover-runbook.md` with a minute-by-minute sequence (per CHECKLIST.md CUT-01)" (line 52). | `docs/migration/cutover-runbook.md` **does not exist**. Only `docs/migration/launch-gate.md` and `docs/migration/vercel-preview-validation.md` are present. A "DNS-flip runbook in audits HTML" is referenced in memory (`project_cutover_next_actions_2026_05_10_pt2.md`) but lives outside `docs/migration/` and is not the canonical artifact. |

**Why it matters:** CUT-01 will FAIL by default. The default-NO-GO condition in the seed prompt is already tripped by this alone, regardless of anything else the audit finds.

### A6. Audit folder layout

| Seed prompt | Current truth |
|---|---|
| References sibling folders `audits/migration-readiness/`, `audits/migration-auditor/`, `audits/site-hygiene/`, `audits/migrate-day-decision/` (lines 18, 31). | Only `audits/migration-readiness/` exists (with `2026-05-11.md` and `2026-05-11-plain-english.md`). `audits/migration-auditor/`, `audits/site-hygiene/`, `audits/migrate-day-decision/` are not yet created. (This diff file is the first artifact written under `audits/migrate-day-decision/`.) |

### A7. Evidence pack path

| Seed prompt / launch-gate | Current truth |
|---|---|
| Launch-gate cites `/migration-evidence-pack/…` as the canonical evidence path. | Actual evidence is at **`/Users/brock/Downloads/migration-evidence-pack 6/`** (note the trailing ` 6` — sixth iteration). Sub-skills that grep for `/migration-evidence-pack/` literally will miss files. Migration-auditor's 2026-05-11 16:24 UTC run resolved this by pointing at the Downloads path. |

### A8. Vercel preview alias discovery instruction

| Seed prompt | Current truth |
|---|---|
| "Vercel preview alias: check `vercel alias ls` for the current production-candidate preview" (line 15). | The current production-candidate alias is **already known**: `https://sparkshark-com-git-main-spark-shark-electric-2b2f3a3a.vercel.app` (verified live with HTTP 200 in the 2026-05-11 16:24 UTC migration-auditor run). Memory `project_cutover_reality_check_2026_05_10.md` also pins this. A fresh `vercel alias ls` call is unnecessary and risks the wrong alias being picked. |

---

## B. Status diffs (things the seed prompt's "default NO-GO" conditions test for — current state of each)

The seed prompt lists ten conditions (lines 41–52) that must ALL be true for a GO. Their current state, as of `0ce83e5`:

| # | Condition | Current state | Source |
|---|---|---|---|
| 1 | `migration-auditor` returns GO band | **FAIL** — most recent run scored 40 / 100, HARD NO-GO | `audits/migration-readiness/2026-05-11.md` |
| 2 | Every launch-gate row Approved or NA-with-reason | **FAIL** — 5 of 9 Approved (1, 2, 3, 5, 6); 4 Not Provided (4 GBP, 7 Tracking IDs, 8 ST scheduler test, 9 Canonical NAP) | `docs/migration/launch-gate.md` |
| 3 | `site-hygiene-audit` zero BLOCKERs | **UNRUN** this session — no `audits/site-hygiene/` artifact exists | repo state |
| 4 | `seo-audit` no indexation-blocking issues on preview | **PARTIAL** — `robots.txt`, `sitemap.xml`, `aggregateRating` already audited inside migration-readiness; one drift: `llms.txt` says `4.9`, `BRAND` + `llms-full.txt` say `4.8` (RANK-08 FAIL) | `audits/migration-readiness/2026-05-11.md` §3 |
| 5 | DNS rollback insurance documented | **PARTIAL** — ROLL-01 (DNS-before screenshots) and ROLL-02 (WPE backup) PASS; ROLL-03 (transcribed values) and ROLL-04 (rollback trigger criteria) FAIL | `audits/migration-readiness/2026-05-11.md` §8 |
| 6 | WordPress redirect-plugin audit complete + WP redirects replicated in `vercel.json` | **PASS** — WPE Web Rules / Rewrite Rules / Access Rules all confirmed empty; `vercel.json` carries 27 redirects | `migration-evidence-pack 6/08-legacy-flanco-entity-audit/wp-engine-redirect-rules/`; `~/Projects/sparkshark-com/vercel.json` |
| 7 | Contact form POSTed end-to-end + Resend delivery confirmed in last 24h | **PASS** — POST at `2026-05-11T16:23:10Z` returned `{"ok":true}`; Resend delivery id `5cb36ea7-…` to `theteam@sparkshark.com` confirmed prior session | `audits/migration-readiness/2026-05-11.md` LEAD-01 |
| 8 | ST scheduler embed renders on live alias `/` and `/contact-us/` | **PASS** — both pages carry the embed with matching `data-api-key` + `data-schedulerid` from `build.py` | `audits/migration-readiness/2026-05-11.md` LEAD-02 |
| 9 | Custom domain attachment verified three ways | **FAIL** — `sparkshark.com` apex is attached to project `spark-fsm` (not `sparkshark-com`); `www.sparkshark.com` has NO project assignment; `sparkshark-com` project is in a different Vercel team. Three vantages (`vercel domains ls`, `vercel domains inspect`, `vercel inspect`) all confirm. Auto-fail trigger fires. | `audits/migration-readiness/2026-05-11.md` §6 (DOM-01 / DOM-02) |
| 10 | gitleaks clean on main | **UNVERIFIED** — `gitleaks` binary not installed; canonical SEC-01 sweep not runnable today | `audits/migration-readiness/2026-05-11.md` SEC-01 |
| 11 | Cutover-day runbook at `docs/migration/cutover-runbook.md` | **FAIL** — file does not exist (see A5) | repo state |

**Net:** four hard fails (1, 2, 9, 11), three partials (4, 5, 10), three passes (6, 7, 8), one unrun (3). Even without a fresh audit, the seed prompt's own logic forces NO-GO today.

---

## C. NEW material finding not yet reflected in the seed prompt

The seed prompt's "decision discipline" list (lines 41–52) does not include a check for **which Vercel project the apex domain is assigned to**. The 2026-05-11 16:24 UTC migration-auditor run discovered that `sparkshark.com` is currently attached to `spark-fsm` (not `sparkshark-com`), and `www.sparkshark.com` has no project assignment at all. If DNS were flipped today, every sparkshark.com request would land on the spark-fsm app, not the marketing site.

Recommendation: add a DOM-01 / DOM-02 row to the decision-discipline list in any future seed prompt. The 9-way custom-domain check is already in `migration-auditor/CHECKLIST.md` (referenced as DOM-01/DOM-02 in §6 of the audit) but is not reflected in the seed prompt's surface-level test list.

---

## D. Items not in dispute (confirmed consistent — no diff)

These are listed so a future reader can see what the diff pass DID check and found unchanged:

- Domain: `sparkshark.com` ✓
- Source stack: WordPress on WP Engine, install name `flancoelectric`, still live ✓ (`migration-evidence-pack 6/12-launch-and-rollback/wp-engine-backup-confirmation-list.png`)
- Project root on disk: `~/Projects/sparkshark-com` ✓
- New phone: `(405) 436-4776` ✓ (`build.py` `BRAND["phone_tel"]` = `+14054364776`; site-wide `tel:` link verified 7-page sample in audit LEAD-03)
- Launch gate file at `docs/migration/launch-gate.md` ✓ (exists, 9 gate items as described)
- Skills installed: `migration-readiness`, `migration-auditor`, `seo-audit`, `ai-seo`, `programmatic-seo`, `site-hygiene-audit` — all six present in `available_skills` for this session ✓
- "Only Brock can mark NA" rule — already in `launch-gate.md` §"Explicit rule" ✓
- Off-limits surfaces list (CLAUDE.md, .env*, api/, BRAND dict, ST IDs, `vercel.json` redirects[], Status cells) — consistent with both repos' CLAUDE.md "Do Not Touch Without Explicit Approval" ✓

---

## E. One-line recommendation (still report-only — no action taken)

Before pasting the seed prompt into a fresh Cowork session, patch lines 13, 16, and 19 of the seed prompt to: target stack = "static HTML built by `build.py` (Python), no Next.js"; new entity = "BSF Investment Group, LLC (DBA Spark Shark Electric)"; drop the uploaded-runbook reference (or re-attach it). Also add a one-line note above the four-step plan: "Apex domain `sparkshark.com` is currently attached to the wrong Vercel project — see `audits/migration-readiness/2026-05-11.md` §6. Fix project attachment before any further go/no-go pass."

---

*End of diff. No files were changed and no audit was run. This file is the only artifact produced this turn.*
