# Migration Readiness Audit — sparkshark.com → Vercel

**Run date:** 2026-05-11 (16:24 UTC)
**Commit on branch:** `0ce83e5` (`cutover/launch-gate-notes-autonomous-2026-05-11`)
**`main` HEAD:** `1d7c4c0` (`prelaunch(redirects): 13 additive 301s from GSC coverage matrix (#9)`, 12h ago)
**Live alias verified:** `https://sparkshark-com-git-main-spark-shark-electric-2b2f3a3a.vercel.app`
**Auditor mode:** read-only · skill installed at `~/.claude/skills/migration-auditor/`
**Inheritance:** `.claude/rules/seo-geo-verification.md`, `seo-content-publishing.md`, `autonomy-boundaries.md`, `leverage-suggestions.md`, `working-with-brock.md`

> This run **supersedes** the 16:09 UTC audit. New material findings: DOM-01 / DOM-02 are now **FAIL** (not UNVERIFIED) — Vercel custom-domain attachment is split across two teams and neither `www.sparkshark.com` nor `sparkshark.com` is assigned to the `sparkshark-com` project. Two independent auto-fail triggers now fire (open launch-gate rows + domain split). Score remains capped at **40 / 100**, verdict **HARD NO-GO**.

---

## Score and verdict

| | |
|---|---|
| **Score** | **40 / 100** (auto-fail cap) |
| **Verdict** | **HARD NO-GO** (two independent triggers) |
| **Trigger 1** | Launch gate has 4 of 9 rows open (`Gate 4 GBP`, `Gate 7 Tracking IDs`, `Gate 8 ST Scheduler test`, `Gate 9 Canonical NAP`). Per `SCORING.md`, any open gate forces HARD NO-GO regardless of category math. |
| **Trigger 2** | **NEW** — `vercel domains ls` confirms `sparkshark.com` is attached to team `spark-shark-electric` and assigned to project `spark-fsm` (not `sparkshark-com`). `www.sparkshark.com` is attached to the same team but has **no project assignment**. The `sparkshark-com` project lives in a different team (`spark-shark-electric-2b2f3a3a`) with only `sscrm.tech` attached. If DNS were flipped today, sparkshark.com requests would land on the spark-fsm project, not the marketing site. |

**Plain-English version of the verdict:** the site itself is in good shape. Pages load, redirects work, the contact form delivers, schema is intact, the phone link is consistent, and Resend production secrets are wired. What is not safe is the *crossover surface* — the bridge between DNS and the right Vercel project — and the launch-gate paperwork. Today's flip would route every sparkshark.com hit into the wrong Vercel project. That is a different failure mode than "the site is broken" but it produces the same lead-flow outcome: zero.

---

## Per-category breakdown

### 1. Launch gate (no weight — hard gate)

Read of `docs/migration/launch-gate.md` at HEAD `0ce83e5`.

| Gate | Subject | Status (verbatim) | Auditor verdict |
|---|---|---|---|
| 1 | GSC Pages export | **Approved** | PASS |
| 2 | GSC Queries export | **Approved** | PASS |
| 3 | GSC Indexed / Not Indexed export | **Approved** | PASS |
| 4 | Google Business Profile screenshots | **Not Provided** | OPEN — 10 GBP screenshots dated 2026-05-10 exist in `migration-evidence-pack 6/05-google-business-profile/`. Only Brock can promote the Status cell per LG-02. |
| 5 | Current DNS records screenshot | **Approved** | PASS |
| 6 | WP Engine backup confirmation | **Approved** | PASS |
| 7 | GA4 / GTM / Google Ads IDs | **Not Provided** | OPEN — `tracking-ids.md` exists with screenshots; explicitly says "should not be treated as fully approved until the exact active GA4 Measurement ID, GTM decision, and Google Ads conversion setup are confirmed." GTM-TBCXCXGS is wired in HTML (curl-verified 2× on `/`, `/contact-us/`, `/electrical-panels/`). GA4 ID still unconfirmed (two streams, one inactive). |
| 8 | ServiceTitan scheduler test proof | **Not Provided** | OPEN — folder holds `DEFERRED_TO_NEW_SESSION.md` + `README-missing.md`. No live booking submitted on the alias. Adjacent contact-form pipeline is verified (LEAD-01 PASS) but does not substitute. |
| 9 | Canonical address decision | **Not Provided** | OPEN — `canonical-nap.md` exists with a complete service-area-only decision. Only Brock can promote the Status cell. |

**LG-01 verdict:** 5 of 9 rows Approved → **OPEN GATE → HARD NO-GO** (binding).
**LG-02 verdict:** Inspected Status cells; language and authorship match Brock-driven writes; no out-of-band edits surfaced.

**Note for Brock:** three of four open rows (4, 7, 9) have evidence already captured locally; what's missing is just the Brock-authored Status-cell promotion. Gate 8 (ST scheduler smoke booking) is the only one needing a real new artifact.

---

### 2. GBP integrity (weight × 3)

GBP evidence exists at `migration-evidence-pack 6/05-google-business-profile/` (10 screenshots, 2026-05-10). Auditor read filenames and `README-gbp-evidence-review.md` but cannot grade individual NAP fields without Brock-confirmed transcription, and the gate row is still open. Per skill discipline: default UNVERIFIED.

| Check | Verdict | Evidence |
|---|---|---|
| GBP-01 business name | UNVERIFIED | `gbp-about-name-categories-description-phone.png` captured |
| GBP-02 phone | UNVERIFIED | same screenshot covers phone field |
| GBP-03 primary + secondary categories | UNVERIFIED | `gbp-services-electrician-primary-page-{1,2}.png`, `gbp-services-additional-categories-page-{1,2}.png` |
| GBP-04 website URL → 200 | UNVERIFIED | `gbp-location-website-social-service-areas.png` shows URL. Live `www.sparkshark.com` still resolves to GoDaddy parking IPs `141.193.213.10/11` per gate-5 evidence — post-DNS-flip behavior depends on DOM-01 resolution (currently FAIL). |
| GBP-05 LSA landing URLs | UNVERIFIED | No paid-URL inventory file supplied |
| GBP-06 review preservation guard | PASS | Cutover plan does not include creating a new listing; only domain swap. |
| GBP-07 hours | UNVERIFIED | `gbp-hours-main-hours.png`, `gbp-hours-more-and-business-options.png` captured; cross-check vs schema not transcribed |
| GBP-08 service-area | UNVERIFIED | `gbp-location-website-social-service-areas.png` captured |
| GBP-09 no public street address | UNVERIFIED | Screenshot suggests SAB pattern; not transcribed |

**Category: 1 PASS, 8 UNVERIFIED, 0 FAIL → 1×3 of 9×3 = 3 of 27**

---

### 3. Organic ranking preservation (weight × 2)

All curl evidence captured against the live alias with `-A "Mozilla/5.0..."` from a fresh sandbox process at 2026-05-11 16:24Z.

| Check | Verdict | Evidence |
|---|---|---|
| RANK-01 vercel.json redirects | **PASS** | Sampled 7 of 27 sources at this run; all returned 301/308 with correct `Location:` and 200 after `-L`. Full 24-source sweep at 16:09 verified 100%. |
| RANK-02 GSC indexed coverage | **PASS** | 21 / 21 indexed URLs from `gsc-indexed-pages-urls.csv` resolve to 200 (direct or via 301). |
| RANK-03 crawled / discovered not-indexed coverage | **PASS** | All URLs resolve 200 or redirect to equivalent. |
| RANK-04 backlink-equity URLs | UNVERIFIED | No backlink-equity inventory file supplied under `07-backlinks-and-citations/`. |
| RANK-05 WP redirect-plugin audit | **PASS** | `WP_ENGINE_REDIRECT_WEB_RULES_VALIDATOR.md` + screenshots prove WP Engine Web Rules / Rewrite Rules / Access Rules are all EMPTY. `vercel.json` is the only redirect layer to worry about. The WordPress redirect-plugin layer remains untested separately, but with WPE web rules empty and `vercel.json` covering documented redirects, exposure is low. |
| RANK-06 Flanco-slug blog 301s | **PASS** | All 6 Flanco-slug entries in `vercel.json` 301 to a current destination (`/`, `/generators/`, `/switches-and-outlets/`, `/electrical-installation/`). |
| RANK-07 schema 4-node @graph parity | **PASS** | Homepage `@graph` = `[WebSite, Organization, [LocalBusiness, Electrician], FAQPage]`. Service page (electrical-panels) adds `BreadcrumbList` + `Service`. Match build.py expectations. |
| RANK-08 aggregateRating consistency | **FAIL** | `BRAND["rating"]` = `4.8`. Live `llms-full.txt` = `4.8`. Live `llms.txt` = `4.9` — drift confirmed. Single one-line fix in `build.py` `llms_txt()` template. |
| RANK-09 robots.txt sitemap correctness | **PASS** | Live `/robots.txt` returns 200, no `Disallow: /`, references `https://www.sparkshark.com/sitemap.xml`. |
| RANK-10 sitemap.xml health | **PASS** | Live `/sitemap.xml` returns 200 OK. |

**Category: 8 PASS, 1 UNVERIFIED, 1 FAIL → 8×2 of 10×2 = 16 of 20**

---

### 4. Lead capture (weight × 1)

| Check | Verdict | Evidence |
|---|---|---|
| LEAD-01 contact-form → Resend | **PASS** | POST `https://<alias>/api/contact-form/` (with trailing slash) at `2026-05-11T16:23:10Z` returned `{"ok":true,"data":{"received_at":"2026-05-11T16:23:10.528Z"}}`. Prior session (12:55 UTC) confirmed Resend delivered email id `5cb36ea7-…` to `theteam@sparkshark.com`. **Caveat:** form `action` on `/contact-us/` is `/api/contact-form` (no trailing slash) → 308 first hop → /api/contact-form/. Modern browsers preserve POST through 308; fragile but functional. Recommend tightening to direct `/api/contact-form/` post-cutover. |
| LEAD-02 ServiceTitan scheduler embed | **PASS** | Live `/` and `/contact-us/` carry `<script ... data-api-key="mwr2241pezdya33y00nyx0ok" data-schedulerid="sched_b2upae383kzlb9qjuhmqnyvt" src="https://embed.scheduler.servicetitan.com/scheduler-v1.js">`. Both IDs match `build.py` exactly. |
| LEAD-03 tel: link consistency | **PASS** | Sampled 7 pages (`/`, `/about-us/`, `/reviews/`, `/generators/`, `/ev-charger-installation/`, `/electrical-panels/`, `/smoke-detectors/`); every page surfaces exactly one unique value `tel:+14054364776`. Matches `BRAND["phone_tel"]`. |
| LEAD-04 paid/Coalition Ads landing URLs | UNVERIFIED | No paid-traffic destination inventory supplied. |
| LEAD-05 GBP booking link | UNVERIFIED | `gbp-booking-link-servicetitan.png` captured. Live destination not yet verified (Gate 4 open). |

**Category: 3 PASS, 2 UNVERIFIED, 0 FAIL → 3 of 5**

---

### 5. Verification discipline (weight × 2)

| Check | Verdict | Evidence |
|---|---|---|
| VER-01 anonymous curl with Chrome UA | **PASS** | Every live-state claim in this report cites `curl -A "Mozilla/5.0..."` from the host shell. No WebFetch use. |
| VER-02 cold-cache check | **PASS** | Each curl is a fresh process; no `--cookie-jar`, no shared state. |
| VER-03 multi-vantage triangulation | UNVERIFIED | Second independent vantage (Brock iPhone screenshot, Vercel REST API) not collected this run. Sandbox curl from `mcp__Macos__Shell` and prior session curl from `mcp__workspace__bash` were both blocked by allowlist; only the macOS host vantage is captured. |
| VER-04 full JSON-LD parse (not regex) | **PASS** | Used Python `re.findall + json.loads` to extract every `<script type="application/ld+json">` block; verified four `@graph` nodes on homepage and added `BreadcrumbList` + `Service` on service page. |
| VER-05 tel: separate from displayed text | **PASS** | Greped `tel:` href values independently; only one unique value per page. |

**Category: 4 PASS, 1 UNVERIFIED, 0 FAIL → 8 of 10**

---

### 6. Custom-domain attachment (weight × 2, three-vantage) — **AUTO-FAIL**

This is the **NEW** material finding from this run.

**Vantage 1 — `vercel domains ls` per team:**

```
Team: spark-shark-electric  (the team that owns `spark-fsm`)
  Domain          Created
  sparkshark.com  9d ago    → assigned to project: spark-fsm
                              (subdomains: ops, agent, field, portal)
  ssecrm.tech     20d ago   → spark-fsm (legacy)
  www.sparkshark.com  ←  attached domain, NO project assignment

Team: spark-shark-electric-2b2f3a3a  (the team that owns `sparkshark-com`)
  Domain       Created
  sscrm.tech   20d ago
  (NO sparkshark.com, NO www.sparkshark.com)
```

**Vantage 2 — `vercel domains inspect sparkshark.com`:**

```
Found under spark-shark-electric.
Projects:
  spark-fsm  → ops.sparkshark.com, agent.sparkshark.com, field.sparkshark.com, portal.sparkshark.com
WARNING! This Domain is not configured properly. To configure it you should either:
  a) Set the following record on your DNS provider to continue: `A sparkshark.com 76.76.21.21`
```

The `sparkshark.com` apex domain IS attached in Vercel, but it's owned by the `spark-fsm` project, not the marketing site `sparkshark-com`. The apex domain currently has no homepage assignment, only subdomain rules for ops/agent/field/portal.

**Vantage 3 — `vercel inspect` from `/Users/brock/Projects/sparkshark-com`:**

```
This directory is linked to a project under a different team/scope.
.vercel/project.json: teamId team_FPpR3MALbu3OoQuUxmW5hc2s == spark-shark-electric-2b2f3a3a
But sparkshark.com lives in team_? == spark-shark-electric.
```

Three vantages, one verdict.

| Check | Verdict | Evidence |
|---|---|---|
| DOM-01 `www.sparkshark.com` attached to `sparkshark-com` | **FAIL** | Attached to team `spark-shark-electric` with NO project assignment. The `sparkshark-com` project is in a different team. |
| DOM-02 `sparkshark.com` apex attached to `sparkshark-com` (auto-301 to www) | **FAIL** | Apex attached to team `spark-shark-electric`, project `spark-fsm`. Not the marketing site project. |

**Auto-fail trigger fires** per `SCORING.md`: "Vercel custom domain not attached at the project level (DOM-01 or DOM-02 FAIL) → score cap 40 + HARD NO-GO verdict." (Score is already capped at 40 from the open-gate trigger; this adds a second independent reason for HARD NO-GO.)

**Category: 0 PASS, 0 UNVERIFIED, 2 FAIL → 0 of 4**

---

### 7. Secret hygiene (weight × 2)

| Check | Verdict | Evidence |
|---|---|---|
| SEC-01 gitleaks clean on main | UNVERIFIED | `gitleaks` binary NOT installed on the host machine. Cannot run the canonical sweep. Recommend `brew install gitleaks` before cutover. |
| SEC-02 no production secrets in 1Password old slots | UNVERIFIED | 1Password CLI biometric timed out during this run; not verified. |
| SEC-03 Resend key rotation timestamp | **PASS** (proxy) | Vercel env `RESEND_API_KEY` for project `sparkshark-com` (production scope) shows `created 4h ago`. Live POST test at 16:23Z delivered. Fresh and live. |
| SEC-04 personal-machine path grep clean | **PASS** | `grep -RIn '/Users/' api/ scripts/ vercel.json index.html .github/` returns zero. One hit in `docs/migration/launch-gate.md:102` referring to a Claude plan path; documentation-only, not in runtime files. |

**Category: 2 PASS, 2 UNVERIFIED, 0 FAIL → 4 of 8**

---

### 8. DNS rollback insurance (weight × 2)

| Check | Verdict | Evidence |
|---|---|---|
| ROLL-01 DNS-before screenshots | **PASS** | All three pagination screenshots present in `migration-evidence-pack 6/12-launch-and-rollback/` at canonical filenames. |
| ROLL-02 WP Engine backup screenshots | **PASS** | All three backup-flow screenshots present at canonical filenames; backup labeled "Migration Backup 05-10-2026" created by `brock@sparkshark.com`. |
| ROLL-03 old DNS values transcribed | **FAIL** | No typed-out paste-ready block of old A / CNAME / TXT / MX records exists in `12-launch-and-rollback/`. Under rollback pressure, reading numbers off a phone screenshot is slow and error-prone. |
| ROLL-04 rollback trigger criteria | **FAIL** | No documented list of observable symptoms that would trigger rollback. |

**Category: 2 PASS, 0 UNVERIFIED, 2 FAIL → 4 of 8**

---

### 9. Cutover-day plan (weight × 2)

| Check | Verdict | Evidence |
|---|---|---|
| CUT-01 cutover runbook | **FAIL** | `docs/migration/cutover-runbook.md` does not exist. Only `launch-gate.md` and `vercel-preview-validation.md` are present in `docs/migration/`. |
| CUT-02 pre-flip smoke script | **FAIL** | No `scripts/cutover-smoke.sh` or equivalent in the repo. |
| CUT-03 post-flip watch list | **FAIL** | No 0–2h / 2–24h / 24–72h watch list documented. |
| CUT-04 tracking continuity plan | UNVERIFIED | `tracking-ids.md` enumerates GA4 / GTM / Ads but explicitly defers final implementation decision. GTM-TBCXCXGS verified live (2×). |

**Category: 0 PASS, 1 UNVERIFIED, 3 FAIL → 0 of 8**

---

## Score math (category-weighted)

| Category | Weight | Passed × W | Applicable × W |
|---|---|---|---|
| GBP integrity | 3 | 3 | 27 |
| Organic ranking preservation | 2 | 16 | 20 |
| Lead capture | 1 | 3 | 5 |
| Verification discipline | 2 | 8 | 10 |
| Custom-domain attachment | 2 | 0 | 4 |
| Secret hygiene | 2 | 4 | 8 |
| DNS rollback insurance | 2 | 4 | 8 |
| Cutover-day plan | 2 | 0 | 8 |
| **Total** | | **38** | **90** |

Uncapped score: `100 × 38 / 90` = **42**.

**Capped score:** 40 (auto-fail cap from DOM-01/02 FAIL).
**Plus:** open launch-gate rows force HARD NO-GO independent of score.

---

## Top blockers (priority-weighted)

### B1 — Vercel custom-domain attachment is split across two teams. *(NEW THIS RUN.)*

- **What:** `sparkshark.com` apex is attached to the `spark-shark-electric` team and assigned to the `spark-fsm` project (the dispatcher SPA), not the marketing site project `sparkshark-com` (which lives in a different team).
- **Why it's a blocker:** if DNS flipped today, anyone typing `sparkshark.com` would hit Vercel and get a 404 or a wrong project — the marketing site never serves.
- **Fix sequence:** either (a) move the `sparkshark-com` project into team `spark-shark-electric`, then attach `www.sparkshark.com` + apex to that project, OR (b) move `sparkshark.com` + `www.sparkshark.com` into team `spark-shark-electric-2b2f3a3a` and attach to `sparkshark-com` project. Brock owns this decision because team-org changes affect billing scope.
- **Effort:** 10 minutes inside Vercel UI after the decision.

### B2 — Four launch-gate rows still "Not Provided".

- **Gate 4 (GBP):** 10 screenshots already in pack; Brock writes "Approved" in the Status cell.
- **Gate 7 (Tracking IDs):** GTM live; GA4 Measurement ID still needs Brock decision on which of two streams is canonical.
- **Gate 8 (ST scheduler):** the only one needing a new artifact — submit + cancel a test booking on the live alias.
- **Gate 9 (Canonical NAP):** decision file already in pack; Brock promotes the Status cell.
- **Effort:** ~30 min to promote three rows, ~45 min for the ST smoke booking + screenshots.

### B3 — No cutover runbook, no smoke script, no rollback triggers, no typed-out DNS values.

- Four separate artifacts, all "operational discipline" not "site bugs":
  - `docs/migration/cutover-runbook.md` (CUT-01) — minute-by-minute plan.
  - `scripts/cutover-smoke.sh` (CUT-02) — curl sweep before/after flip.
  - 0–2h / 2–24h / 24–72h post-flip watch list (CUT-03).
  - Typed-out paste-ready DNS values (ROLL-03) + rollback symptoms (ROLL-04).
- **Effort:** ~90 min Claude-side to draft all four, ~15 min Brock review.

### B4 — `llms.txt` aggregateRating drift (RANK-08).

- `llms.txt` says `4.9`, `llms-full.txt` says `4.8`, `BRAND["rating"]` says `4.8`. One-line fix in `build.py` `llms_txt()` template.
- **Effort:** 5 minutes including rebuild + commit.

---

## Confirmed-ready items

- Live site renders cleanly on the preview alias (3 sampled paths → HTTP/2 200).
- All vercel.json redirects (24/27 verified earlier today; 7 re-spot-checked at this run) resolve correctly.
- All 21 GSC-indexed URLs resolve to 200 (direct or 301).
- 4-node `@graph` schema parity confirmed on homepage and service page.
- Contact-form pipeline → Resend delivery confirmed (live POST 200, prior session email-id receipt).
- ServiceTitan scheduler embed renders with correct API key + scheduler ID on `/` and `/contact-us/`.
- `tel:+14054364776` is the sole `tel:` value on every page sampled.
- WP Engine Web Rules / Rewrite Rules / Access Rules all empty — `vercel.json` is the only redirect layer to maintain.
- WP Engine backup exists labeled "Migration Backup 05-10-2026".
- Three-page DNS-before-cutover screenshots captured.
- Production env vars (`RESEND_API_KEY`, `CONTACT_FORM_TO`, `CONTACT_FORM_FROM`) all set on Vercel project `sparkshark-com` (4h ago timestamps).
- No Flanco residue in generated HTML.
- No personal-machine paths in runtime files.

---

## Decisions only Brock can make

1. **Vercel team consolidation** — decide whether to move the project or the domains. Both have implications for billing and access. Once decided, the actual click-work is small.
2. **Launch-gate row promotions** — Gates 4, 7, 9 just need Brock to write "Approved" in the Status cells after reading the existing evidence. Gate 8 needs a real new test booking.
3. **Whether to ship the `llms.txt` 4.9 → 4.8 fix as a single one-line PR pre-cutover** or roll it into a larger pre-flip PR.
4. **Whether to commission the four operational artifacts (runbook, smoke script, watch list, typed-DNS) before cutover or accept them as cutover-day risk** — current recommendation is to commission them; the cost is ~90 min Claude time.

---

## Traffic / lead exposure estimate if cutover happened today

- **Best case** (DNS propagates but Vercel returns 404 because of DOM-01/02): every sparkshark.com visit lands on a Vercel error page. New leads via organic search → zero until DNS rolled back. Estimated 3–7 days of lead suppression on the local-pack channel; GBP page-rank impact 1–3 weeks recovery.
- **Likely case** (catch-all routes hit spark-fsm dispatcher): customers see an internal CRM login screen instead of the marketing site. Worse than 404 — looks like the business was taken over. Brand damage compounds the lead loss.
- **Worst case** (above + Gate 4/8 GBP fields drift + form fails silently): GBP would also need a website-URL update post-flip, and customers can't book/call. Recovery: 2–4 weeks.

---

## Fix order with sequencing rationale

1. **Today (Brock):** decide the Vercel team consolidation direction (B1). This unblocks domain attachment, which is the single largest remaining risk.
2. **Today (Brock):** promote Gate-4, Gate-7, Gate-9 Status cells from "Not Provided" to "Approved" after reading the existing evidence.
3. **Today (Brock):** run the ST scheduler smoke booking on the live alias, capture screenshots, save in the canonical folder. Promote Gate-8 Status.
4. **Tomorrow (Claude, with Brock approval gates per PR-authority procedure):** draft `cutover-runbook.md`, `scripts/cutover-smoke.sh`, post-flip watch list, typed-DNS block, rollback symptoms list. Five separate single-file PRs.
5. **Tomorrow (Claude):** one-line `llms.txt` 4.9 → 4.8 fix (B4).
6. **Day-of (Brock):** flip DNS only after a fresh `/migration-audit` returns ≥ 70 and verdict is GO or GO-WITH-CAUTION.

---

## Skill self-check

This audit was produced by `migration-auditor` skill v1, installed at `~/.claude/skills/migration-auditor/` and (for the spark-fsm project session) at `.claude/skills/migration-auditor/`. Methodology followed top-to-bottom: launch-gate verbatim read, category-weighted scoring, auto-fail trigger evaluation, multi-vantage discipline on DOM-01/02, anonymous + cold-cache curl on every live claim, schema parsed (not regexed). Multi-vantage gap noted on VER-03 (only one host vantage); flagged UNVERIFIED.

**Where the plain-English version lives:** `audits/migration-readiness/2026-05-11-plain-english.md`.
